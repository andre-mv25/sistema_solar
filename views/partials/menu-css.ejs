<style>
.navbar {
    position: fixed; top: 16px; left: 50%;
    transform: translateX(-50%);
    width: calc(100% - 32px); max-width: 1060px;
    background: rgba(8, 10, 28, 0.75);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 18px;
    z-index: 1000;
    padding: 0 18px;
    box-shadow: 0 8px 40px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.04);
}

.nav-container {
    display: flex; align-items: center;
    justify-content: space-between;
    height: 56px;
}

.nav-logo {
    display: flex; align-items: center;
    gap: 10px; text-decoration: none;
    position: relative;
}

.sun-icon {
    width: 28px; height: 28px;
    position: relative; display: flex;
    align-items: center; justify-content: center;
    flex-shrink: 0;
}

.sun-core {
    width: 12px; height: 12px;
    background: #f9d423;
    border-radius: 50%;
    box-shadow: 0 0 12px rgba(249, 212, 35, 0.5);
    animation: sunPulse 3s ease-in-out infinite;
}

@keyframes sunPulse {
    0%, 100% { box-shadow: 0 0 12px rgba(249, 212, 35, 0.5); transform: scale(1); }
    50% { box-shadow: 0 0 20px rgba(249, 212, 35, 0.8); transform: scale(1.1); }
}

.sun-ray {
    position: absolute; top: 50%; left: 50%;
    width: 20px; height: 2px;
    background: linear-gradient(90deg, rgba(249, 212, 35, 0.4), transparent);
    border-radius: 1px;
    transform-origin: left center;
}

.sun-ray:nth-child(2) { transform: translate(-50%, -50%) rotate(0deg) translateX(7px); }
.sun-ray:nth-child(3) { transform: translate(-50%, -50%) rotate(45deg) translateX(7px); }
.sun-ray:nth-child(4) { transform: translate(-50%, -50%) rotate(90deg) translateX(7px); }
.sun-ray:nth-child(5) { transform: translate(-50%, -50%) rotate(135deg) translateX(7px); }
.sun-ray:nth-child(6) { transform: translate(-50%, -50%) rotate(180deg) translateX(7px); }
.sun-ray:nth-child(7) { transform: translate(-50%, -50%) rotate(225deg) translateX(7px); }
.sun-ray:nth-child(8) { transform: translate(-50%, -50%) rotate(270deg) translateX(7px); }
.sun-ray:nth-child(9) { transform: translate(-50%, -50%) rotate(315deg) translateX(7px); }

@keyframes orbitRotate {
    from { transform: translate(-50%, -50%) rotate(0deg); }
    to { transform: translate(-50%, -50%) rotate(360deg); }
}

.sun-ray {
    animation: rayFade 3s ease-in-out infinite;
}

.sun-ray:nth-child(2) { animation-delay: 0s; }
.sun-ray:nth-child(3) { animation-delay: 0.375s; }
.sun-ray:nth-child(4) { animation-delay: 0.75s; }
.sun-ray:nth-child(5) { animation-delay: 1.125s; }
.sun-ray:nth-child(6) { animation-delay: 1.5s; }
.sun-ray:nth-child(7) { animation-delay: 1.875s; }
.sun-ray:nth-child(8) { animation-delay: 2.25s; }
.sun-ray:nth-child(9) { animation-delay: 2.625s; }

@keyframes rayFade {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 1; }
}

.logo-text {
    font-size: 1rem; font-weight: 600;
    color: #fff; letter-spacing: 1.5px;
}

.logo-accent {
    color: #f9d423;
}

.nav-links {
    display: flex; list-style: none;
    gap: 2px; align-items: center;
    margin: 0; padding: 0;
}

.nav-links > li > a {
    text-decoration: none; color: rgba(255, 255, 255, 0.5);
    padding: 6px 14px; border-radius: 10px;
    font-size: 0.85rem; font-weight: 450;
    letter-spacing: 0.3px; white-space: nowrap;
    position: relative;
    transition: all 0.25s ease;
    display: flex; align-items: center;
    gap: 6px;
}

.nav-links > li > a:hover {
    color: rgba(255, 255, 255, 0.9);
    background: rgba(255, 255, 255, 0.04);
}

.nav-links > li > a.active {
    color: #f9d423;
    background: rgba(249, 212, 35, 0.06);
}

.nav-links > li > a .indicator {
    width: 4px; height: 4px;
    border-radius: 50%;
    background: #f9d423;
    opacity: 0;
    transform: scale(0);
    transition: all 0.3s ease;
}

.nav-links > li > a.active .indicator {
    opacity: 1;
    transform: scale(1);
}

.nav-links > li > a:hover .indicator {
    opacity: 0.5;
    transform: scale(1);
}

.dropdown { position: relative; }

.dropdown-toggle {
    cursor: pointer; display: flex;
    align-items: center; gap: 4px;
}

.arrow {
    font-size: 8px; display: inline-block;
    transition: transform 0.35s ease;
    opacity: 0.5;
    line-height: 1;
}

.dropdown:hover .arrow { transform: rotate(180deg); opacity: 1; }

.dropdown-menu {
    position: absolute; top: calc(100% + 10px); left: 50%;
    transform: translateX(-50%) translateY(8px) scale(0.95);
    background: rgba(8, 10, 28, 0.9);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 14px; padding: 5px;
    min-width: 190px; list-style: none;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
    opacity: 0; visibility: hidden;
    transform-origin: top center;
    transition: opacity 0.2s ease, transform 0.25s ease, visibility 0.2s;
}

.dropdown-menu::before {
    content: ''; position: absolute;
    top: -5px; left: 50%; transform: translateX(-50%) rotate(45deg);
    width: 8px; height: 8px;
    background: rgba(8, 10, 28, 0.9);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-bottom: none; border-right: none;
}

.dropdown:hover .dropdown-menu {
    opacity: 1; visibility: visible;
    transform: translateX(-50%) translateY(0) scale(1);
}

.dropdown-menu a {
    display: flex; align-items: center;
    gap: 10px; text-decoration: none;
    color: rgba(255, 255, 255, 0.55);
    padding: 8px 12px; border-radius: 9px;
    font-size: 0.83rem; font-weight: 450;
    transition: all 0.2s ease;
}

.dropdown-menu a:hover {
    background: rgba(255, 255, 255, 0.04);
    color: #fff;
    padding-left: 16px;
}

.dropdown-menu a.active {
    color: #f9d423;
    background: rgba(249, 212, 35, 0.05);
}

.dot {
    width: 6px; height: 6px; border-radius: 50%;
    flex-shrink: 0;
    transition: transform 0.2s ease;
}

.dropdown-menu a:hover .dot { transform: scale(1.4); }

.nav-toggle {
    display: none; flex-direction: column;
    gap: 5px; background: none; border: none;
    cursor: pointer; padding: 4px;
    z-index: 1001;
}

.nav-toggle span {
    display: block; width: 20px; height: 1.5px;
    background: rgba(255, 255, 255, 0.6);
    border-radius: 2px;
    transition: all 0.3s ease;
}

.nav-toggle.active span:nth-child(1) { transform: rotate(45deg) translate(4px, 4px); background: #f9d423; }
.nav-toggle.active span:nth-child(2) { opacity: 0; }
.nav-toggle.active span:nth-child(3) { transform: rotate(-45deg) translate(4px, -4px); background: #f9d423; }

@media (max-width: 768px) {
    .navbar {
        top: 12px;
        width: calc(100% - 24px);
        padding: 0 14px;
        border-radius: 14px;
    }

    .nav-toggle { display: flex; }

    .nav-overlay {
        position: fixed; top: 0; left: 0;
        width: 100%; height: 100%;
        background: rgba(0, 0, 0, 0.6);
        backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px);
        z-index: 999;
        opacity: 0; visibility: hidden;
        transition: opacity 0.3s ease, visibility 0.3s ease;
    }

    .nav-overlay.show { opacity: 1; visibility: visible; }

    .nav-links {
        position: fixed; top: 0; right: -300px;
        width: 280px; height: 100vh;
        flex-direction: column;
        background: rgba(8, 10, 28, 0.98);
        backdrop-filter: blur(24px);
        -webkit-backdrop-filter: blur(24px);
        border-left: 1px solid rgba(255, 255, 255, 0.06);
        padding: 100px 20px 32px;
        gap: 2px;
        z-index: 1000;
        margin: 0;
        transition: right 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .nav-links.open { right: 0; }

    .nav-links > li > a {
        width: 100%; padding: 10px 14px;
        border-radius: 10px;
        font-size: 0.95rem;
    }

    .dropdown-menu {
        position: static;
        opacity: 1; visibility: visible;
        transform: none;
        background: rgba(255, 255, 255, 0.02);
        border: none;
        box-shadow: none;
        padding: 4px 0 4px 12px;
        border-radius: 10px;
        display: none;
        margin-top: 2px;
    }

    .dropdown-menu::before { display: none; }

    .dropdown.open .dropdown-menu { display: block; }

    .nav-links > li > a .indicator { display: none; }
}
</style>
